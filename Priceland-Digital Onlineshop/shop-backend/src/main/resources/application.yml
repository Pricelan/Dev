spring:
  datasource:
    url: jdbc:mysql://localhost:3306/priceland?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: priceland
    password: kdls-D344-df!d
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true

server:
  port: 8080
  servlet:
    session:
      cookie:
        same-site: lax
        path: /
        http-only: true
        secure: false  # Wichtig für localhost ohne HTTPS